import styles from "./Footer.module.css"

const Footer = () => {
    return(
        <footer className={styles.footer}>
            <p>Request-broker by mynickleo</p>
        </footer>
    )
}

export default Footer